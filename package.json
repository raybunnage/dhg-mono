{
  "name": "dhg-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "copy-app": "chmod +x scripts/app-management/copy-lovable-app.sh && ./scripts/app-management/copy-lovable-app.sh"
  },
  "devDependencies": {
    "netlify-cli": "^18.0.4",
    "turbo": "^1.12.4"
  }
}