// This file re-exports the config from @dhg/shared for backward compatibility
// It will be deleted once all imports are updated to use @dhg/shared directly
import { config } from "@dhg/shared/utils";

export default config;
